## What does this PR do?

Brief description of the change.

## Checklist

- [ ] `cargo build` — zero warnings
- [ ] `cargo test` — all pass
- [ ] `cargo build --features telegram` — compiles (if touching shared code)
- [ ] Updated README if adding user-facing features
